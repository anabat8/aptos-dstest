# First time cloning

`git clone --recurse-submodules git@github.com:anabat8/aptos-dstest.git`

If you need to pull the submodules: 

`git submodule update --init --recursive`

# Keeping submodules up to date later

`git pull --recurse-submodules`
